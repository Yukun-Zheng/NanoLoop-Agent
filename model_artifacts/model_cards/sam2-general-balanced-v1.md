# sam2-general-balanced-v1

Status: **unavailable**. Neither a checkpoint nor the optional SAM2 runtime is distributed here.

This declaration reserves box-prompted and automatic-mask SAM2 integration. A model handoff must
pin a compatible runtime/config/checkpoint set, record the checkpoint SHA-256 and license, document
hardware requirements, and add fixture-backed acceptance tests. SAM2 may return no calibrated
per-pixel probability artifact; that absence is represented explicitly rather than fabricated.

No accuracy or material applicability claim has been established yet.
